<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hu.bme.aut.hajfoglalo">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="Hajfoglaló"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hajfoglalo">
        <activity
            android:name=".Contacts"
            android:label="Elérhetőség"
            android:exported="true" />
        <activity
            android:name=".SavedAppointments"
            android:exported="true" />
        <activity
            android:name=".AppointmentActivity"
            android:label="Időpontfoglalás"
            android:exported="true" />
        <activity
            android:name=".HairServicesActivity"
            android:label="Szolgáltatások"
            android:exported="true" />
        <activity
            android:name=".HairLengthActivity"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>